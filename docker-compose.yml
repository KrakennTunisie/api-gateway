services:
  app:
    build:
      context: .
    container_name: gateway-server
    ports:
      - "8089:8089"
    restart: unless-stopped
    environment:
      SPRING_PROFILES_ACTIVE: prod
    networks:
      - ms-net
networks:
  ms-net:
    external: true